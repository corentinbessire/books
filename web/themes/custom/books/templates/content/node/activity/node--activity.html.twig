<div class="flex flex-row justify-between items-center w-full p-4 rounded-xl bg-gray-900 text-white">
    <div >
      {{ content.extra_field_book_cover }}
    </div>
  <div>
    <h3 class="mb-3 font-bold text-xl">{{ label }}</h3>
  </div>
    <div class="m-4">
      <p class="mb-3">{{ 'Started' |t }} {{ content.field_start_date }}</p>
      <p class="mb-3">{{ 'Finished' |t }} {{ content.field_end_date }}</p>
    </div>
  <div>
    {{ content.field_status }}
  </div>
</div>

