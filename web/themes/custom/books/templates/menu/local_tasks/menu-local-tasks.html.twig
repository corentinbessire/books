<div class="bg-white my-2">
  {% if primary %}
    <h2 class="visually-hidden">{{ 'Primary tabs'|t }}</h2>
    <nav class="flex flex-col sm:flex-row">
      {{ primary }}
    </nav>
  {% endif %}
  {% if secondary %}
    <h2 class="visually-hidden">{{ 'Secondary tabs'|t }}</h2>
    <nav class="flex flex-col sm:flex-row bg-gray-200">
      {{ primary }}
    </nav>
  {% endif %}
</div>
