name: books
recipe: drupal9
config:
  webroot: web
services:
  appserver:
    type: php:8.0
  node:
    type: node:14
    build:
      - npm install
proxy:
  appserver:
    - books.local
tooling:
  npm:
    service: node
  node:
    service: node
